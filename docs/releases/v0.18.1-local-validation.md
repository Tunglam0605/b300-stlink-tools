# v0.18.1 local validation — 2026-09-05

Local software gates passed before branch publication. CI and official release
gates must additionally run on the committed candidate SHA.

- 111 unittest modules completed with PASS verdicts through the canonical
  per-module/split-case runner. Modules changed during review were rerun.
- Windows GUI and CLI bundles built from the candidate worktree; CLI reports
  0.18.1 consistently and its complete runtime manifest validates.
- Inno integration reproduced the original missing-runtime failure before the
  fix. Early and late forced failures now preserve tree hashes, including stale
  old files; successful upgrade removes stale files and retains user data.
- Candidate installer SHA-256:
  `e315a4f13618a41aac71da6abd8465710c1c2250c880b7186bf80c624e3e0dcd`.
  Fresh install, OpenOCD/GDB execution, both rollback hash comparisons,
  post-rollback GUI smoke, successful upgrade and cleanup passed.
- Corrupt runtime-manifest payload is rejected before mutation. An existing
  interrupted backup is preserved and blocks further installation.
- Real GUI login construction and cancellation passed without creating a
  transport. Native Qt teardown warnings in some tests were accepted only when
  the canonical runner recorded a PASS sentinel; timeouts were not accepted.

## Remote incident evidence

The user authorized debugging a test board on Gateway 192.168.1.158 without
flashing. SSH was reachable, but OpenOCD 3333/6666 was not listening. Starting the
existing canonical B300 Gateway restored the existing Client tunnel 43328.
AXF samples matched 4/4; GDB attached, read PC/SP and detached successfully;
target state was RUNNING before and after. The RW_IRAM1/2 warnings remained and
did not prevent that attach.

Gateway used its existing 0.16 CLI; this is incident/compatibility evidence, not
an exact-release Gateway acceptance result. A later candidate preflight correctly
refused an absent listener without a forward. Further GDB tests stopped after
the user's VS Code acquired the single debugger connection.

No flash, mass erase, Option Bytes changes or application provisioning occurred.
HW-P1-001 stays open: B300 APPLICATION ACCEPTANCE remains DEFERRED.

## Preserved local evidence

Detailed logs, hash maps and installer verdicts are under ignored `release/`.
The supplied main checkout's pre-existing untracked hardware logs and
v0.18.0 validation document were preserved. The release worktree is retained.
