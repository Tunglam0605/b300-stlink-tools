# B300 ST-Link Tools v0.14.1 — Release Candidate Acceptance

Date: `2026-09-03`

Branch: `release/v0.14.1`

Candidate baseline: `f0547fa22bc32695f7630208f78d26231758e0d8`

Current decision: **DO NOT PUBLISH**. The release candidate has not yet been pushed and validated as one exact final SHA through the cross-platform CI/package gates, and the physical Windows/Ubuntu SSH field gates are blocked.

## Software evidence

| Gate | Evidence | Status |
|---|---|---|
| Windows isolated-module regression on `f0547fa` | CI-equivalent PowerShell runner; final sentinel `ALL_MODULES_PASS`; `tests.test_gui_smoke` 26/26 and `tests.test_gui_updater` 14/14 | PASS |
| Python compile | `python -m compileall -q b300_core b300_gui`, exit 0 | PASS |
| Patch whitespace | `git diff --check`, exit 0 before review fixes | PASS |
| Source version/tag consistency | `python -m scripts.release.validate_version --check-tag v0.14.1` printed `0.14.1`, exit 0 | PASS |
| Independent review | Initial review of `4897b7d..f0547fa` found two Important GUI issues and one Minor Windows packaging issue; re-review of the fixes found no remaining Critical/Important/Minor issue | PASS |
| Focused review fixes | target invalidation, unsupported target, compact 760×460 toolbar, policy documentation, Windows dark asset, and changelog tests | PASS |
| Final full regression after review fixes | CI-equivalent isolated-module runner printed `ALL_MODULES_PASS`; GUI smoke 28/28, updater 14/14 | PASS |
| Agent skill install/copy integrity | Temporary install completed; all 3 skill files matched source SHA-256 and temporary files were removed | PASS |

## Cross-platform CI and native packages

| Platform | Current evidence for final v0.14.1 SHA | Status |
|---|---|---|
| Windows x64 | Local source tests only; final branch commits are not pushed | PENDING |
| Ubuntu x64 | Previous CI run `33710783538` on `4897b7d` timed out in GUI smoke; it predates the Qt isolation fix | PENDING |
| Ubuntu ARM64 | Previous CI run `33710783538` on `4897b7d` timed out in GUI smoke; it predates the Qt isolation fix | PENDING |
| Development packages | Run `33710820746` succeeded before the final local commits and therefore is not release evidence for the final SHA | STALE |

## Windows hardware preflight

Command: `python b300_stlink.py doctor --json`

- GDB runtime: `PASS`.
- OpenOCD trusted runtime: `PASS`.
- Probe discovery: `NO_PROBE`.
- Target, WRP/RDP, vector, AppMeta, flash and debug checks: skipped because no ST-Link was connected.
- Result: `BLOCKED`; no erase, program, reset, halt, Option Bytes or register write was attempted.

Candidate artifacts previously used for B300 field acceptance exist locally but are not yet authorized for a v0.14.1 destructive transaction:

- `C:\Users\Admin\Documents\STM32\B300-Main-Custom\Objects\F407\Main_V2_F407.hex`
- `C:\Users\Admin\Documents\STM32\B300-Main-Custom\Objects\F407\Main_V2_F407.axf`

The owner must explicitly authorize the exact board, probe and HEX in the current session before the real flash command can run.

## Windows-to-Ubuntu SSH preflight

Saved Client profile: managed identity and strict host trust ready for `admin@192.168.1.145:22`.

- `gateway status`: local profile/key/trust ready.
- `gateway connect-check`: `SSH_CONNECT_FAILED` with password authentication disabled and no debug ports exposed.
- Network diagnostic: host `192.168.1.145` responds to ping, but TCP/22 does not accept a connection from Windows `192.168.1.114`.
- Windows local Gateway doctor: SSH server ready; `127.0.0.1:3333` and `127.0.0.1:6666` free; `NO_PROBE` blocks Gateway start.
- Result: `BLOCKED` until Ubuntu starts/allows OpenSSH TCP/22 and the ST-Link/board is connected to the intended Gateway.

## Remaining mandatory gates

- Commit and push the exact final v0.14.1 SHA.
- Require green CI on Windows x64, Ubuntu x64 and Ubuntu ARM64 for that SHA.
- Build and smoke-test all native artifacts for that SHA.
- Run authorized Windows hardware flash, AppMeta confirmation, health and debug lifecycle tests.
- Run real two-machine SSH Live Monitor, interactive debug, reconnect and cleanup tests.
- Validate clean-machine automatic setup and the installed AI skill.
- Only then create and push tag `v0.14.1`, publish assets, and verify signed public manifests.
