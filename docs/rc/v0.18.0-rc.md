# v0.18.0 release candidate validation

This branch exists only to trigger the non-publishing development package workflow for the exact v0.18.0 integration candidate.

Validation target:
- Windows x64 package build and fresh-install smoke
- Ubuntu x64 package build
- Ubuntu ARM64 package build
- bundled OpenOCD
- bundled managed ARM GDB, including Linux libexec host dependencies
- packaged/fresh-installed GDB execution smoke
- GUI/CLI smoke tests
- v0.18 size budgets

Do not treat this marker as production release notes. Production notes live in `docs/releases/0.18.0.md`.
